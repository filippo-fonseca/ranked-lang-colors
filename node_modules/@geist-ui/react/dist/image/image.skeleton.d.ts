import React from 'react';
interface Props {
    opacity: number;
}
declare const defaultProps: {
    opacity: number;
};
export declare type ImageSkeletonProps = Props & typeof defaultProps;
declare const _default: React.ComponentType<Partial<{
    opacity: number;
}> & Pick<ImageSkeletonProps, never>>;
export default _default;
